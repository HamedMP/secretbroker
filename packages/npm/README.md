# SecretBroker npm launcher

This package selects and executes the signed SecretBroker binary for the current platform. It contains no secret-storage implementation in JavaScript and does not compile native code during installation.

```sh
npx -y secretbroker@0.1.0 --help
```

Pin an exact reviewed version. The platform packages are published alongside this launcher.
